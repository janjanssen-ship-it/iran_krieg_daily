# Old Workflow Archive

This directory preserves the retired local automation setup that used macOS `launchd`.

Contents:

- `launchd/LAUNCHD_COMMANDS.md`
- `launchd/ch.srf.iran-krieg.reduce-points.plist`

The active production automation now lives in:

- `.github/workflows/iran-krieg-daily.yml`
